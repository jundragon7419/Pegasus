# CLAUDE.md

광운대학교 아마야구부 **KWU Pegasus** 웹사이트 재구축 프로젝트.

세션을 시작하면 **먼저 [`README.md`](README.md) 를 읽는다** — 실행 방법, 목 계정,
명령 목록이 거기 있다. 이 파일은 작업할 때 지켜야 할 규칙만 담는다.

> **바로 다음 작업은 투표 시스템 개선이고, 아이디어는 사용자가 직접 준다.**
> 임의로 설계하지 말고 사용자의 구상을 먼저 듣는다. 현재 구현과 아이디어가
> 부딪힐 만한 제약은 `docs/implementation-status.md` 의
> **"예정 — 투표 시스템 개선"** 절에 있다.

## 1. 이 프로젝트의 성격

구 사이트를 **그대로 옮기는 것이 아니다.** [`site-blueprint.md`](site-blueprint.md)
§12 에 구 구현의 결함 26건이 정리되어 있고, **재구축의 목적은 그것을 고치는 것**이다.
청사진의 서술이 곧 사양이 아니다 — §12 가 지적한 부분은 청사진 본문과 어긋나더라도
고친 쪽이 맞다.

지금까지 내린 결정과 그 근거는 [`docs/implementation-status.md`](docs/implementation-status.md)
에 있다. **기존 결정을 뒤집기 전에 그 근거를 먼저 읽는다.**

## 2. 검증 없이 "됐다"고 하지 않는다

이 프로젝트의 기본 규칙이다. 주장하는 것은 재고, 잰 것만 말한다.

- 대비를 말하면 **계산해서** 확인한다
- 번들에 무엇이 없다고 하면 **문자열로 찾아본다**
- 화면이 동작한다고 하면 **브라우저로 걸어본다** — `curl` 은 SPA 라우트에서 전부 200 이라 아무것도 증명하지 못한다
- 테스트를 추가하면 **변이 테스트**로 그 테스트가 회귀를 실제로 잡는지 확인한다

작업을 마치기 전에 `npm run verify` 를 돌린다.

> 이렇게 해서만 잡힌 결함이 여럿이다. StrictMode 이중 효과로 로그인 상태에서
> 모든 보호 라우트가 `/401` 로 튀던 문제, 탈퇴 직후 `/401` 로 가던
> transition 우선순위 문제, 목록이 낡았을 때 승인이 조용히 성공하던 문제.

## 3. 목(MSW)은 계약이다

목은 화면을 그리기 위한 임시 데이터가 아니라 **백엔드가 구현할 API 계약**이다.

- 권한은 **서버가 판정한다.** 화면에서 버튼을 감추는 것은 편의일 뿐이고, 목이 실제로 401/403/409 를 반환해야 한다. 프론트가 협조해야만 성립하는 설계는 목이 먼저 거절한다
- 판정 함수는 `src/lib/` 에 두고 **화면과 목이 같은 함수를 쓴다.** 두 곳에 규칙을 적으면 조용히 어긋난다
- 목이 증명하지 못하는 것(트랜잭션·동시성·FK CASCADE·해싱·rate limit·메일·외부 API)은 `docs/implementation-status.md` 에 명시되어 있다. **"목에서 됐으니 됐다"고 넘기지 않는다**

## 4. 픽스처는 기능을 시험할 수 있어야 한다

**같은 결함이 네 번 나왔다.** 게시글 픽스처가 존재하지 않는 계정을 가리켜 "소유자만
수정"을 확인할 수 없었고, 로스터의 `userId` 가 전부 null 이라 이력이 빈 화면이었고,
계정이 8명뿐이라 관리자 표가 1행씩이었고, 동명이인이 없어 등번호 예외 경로가 한 번도
실행되지 않았다.

기능을 추가하면 **그 기능의 예외 경로를 실행하는 데이터가 픽스처에 있는지** 확인한다.
`src/mocks/fixtures/users.test.ts` 가 이것을 일부 고정한다.

**개인정보**: 구 시드(`C:\code\WEB\KWU-Pegasus\...\sql\seeds\`)에는 실명과 실제 학번이
들어 있다. 이 저장소는 **공개 원격**이 붙어 있으므로 절대 옮기지 않는다. 픽스처의
학번은 전부 가공값이며 **5번째 자리 `9`** 가 합성값 표식이다.

## 5. 코드 규칙

- `src/lib/` 는 **프레임워크를 import 하지 않는다** — 백엔드가 그대로 가져간다. `shared-purity.test.ts` 가 막는다
- 날짜는 `src/lib/date.ts` 를 쓴다. `toISOString().slice(0, 10)` 은 **UTC** 라 한국에서 오전 9시 이전에 전날이 나온다
- 디자인 토큰(`src/styles/design-tokens.css`)은 **스크립트 생성물**이다. 손으로 고치지 않는다
- 팔레트를 늘리지 않는다. 상태 구분은 기존 토큰 + **라벨을 항상 함께** 둔다(색만으로 구분하지 않는다)
- 컴포넌트 파일에서 컴포넌트가 아닌 것을 export 하지 않는다(Fast Refresh)

## 6. 커밋

**커밋과 푸시는 사용자가 직접 한다.** 요청받지 않는 한 하지 않는다.
