import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function Roster() {
  return (
    <PagePlaceholder
      title="선수단"
      phase="6-2"
      note="연도별 카드 그리드. 연도 탭, 역할 필터, 이름·번호 검색, 배지(감독·회장·매니저·영구결번)."
    />
  )
}
