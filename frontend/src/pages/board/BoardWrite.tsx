import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function BoardWrite() {
  return (
    <PagePlaceholder
      title="글쓰기"
      phase="6-6"
      note="유형·상단 고정·투표 설정. 매니저 전용 유형은 권한에 따라 감춘다."
    />
  )
}
